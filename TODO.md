# TODO: Fix Null Safety for toLowerCase() Calls

- [x] Edit lib/screens/admin/crud/crud_user/user_screen.dart: Change `user.nama.toLowerCase()` to `user.nama?.toLowerCase() ?? ''`
- [x] Edit lib/screens/admin/crud/crud_alat/alat_screen.dart: Change `alat.namaAlat.toLowerCase()` to `alat.namaAlat?.toLowerCase() ?? ''`
